    window-rule {
        geometry-corner-radius 20 // Set every window radius to 20
        clip-to-geometry true
    }

    // ─── App → Workspace Affinities ───
    // Verify app-ids with: niri msg windows

    // T — Terminals
    window-rule {
        match app-id=r#"ghostty|com\.mitchellh\.ghostty"#
        open-on-workspace "T"
        default-column-width { proportion 1.0; }
    }
    window-rule {
        match app-id=r#"wezterm|org\.wezfurlong\.wezterm"#
        open-on-workspace "T"
    }

    // B — Browsers
    window-rule {
        match app-id=r#"zen|zen-browser"#
        open-on-workspace "B"
        default-column-width { proportion 1.0; }
    }

    // 1 — Media
    window-rule {
        match app-id="spotify"
        open-on-workspace "1"
        default-column-width { proportion 1.0; }
    }
    
    window-rule {
        match app-id="calibre-gui"
        open-on-workspace "R"
        default-column-width { proportion 1.0; }
    }

    // N — Notes
    window-rule {
        match app-id=r#"obsidian|md\.obsidian"#
        open-on-workspace "N"
    }

    // E — Editor
    window-rule {
        match app-id=r#"jetbrains-rider"#
        open-on-workspace "E"
    }

    // M — Messaging
    window-rule {
        match app-id=r#"teams|com\.microsoft\.Teams"#
        open-on-workspace "M"
    }

    // S — Slack
    window-rule {
        match app-id=r#"Slack|slack"#
        open-on-workspace "S"
    }

    window-rule {
        match app-id=r#"vesktop"#
        open-on-workspace "S"
    }
    
    // D — Database
    window-rule {
        match app-id=r#"mongodb-compass|com\.mongodb\.compass"#
        open-on-workspace "D"
    }

    // G — Gaming
    window-rule {
        match app-id=r#"hero-launcher|herolauncher|faugus-launcher"#
        open-on-workspace "G"
    }
    window-rule {
        match app-id="lutris"
        open-on-workspace "G"
    }
    window-rule {
        match app-id="steam"
        open-on-workspace "G"
    }
    window-rule {
        match app-id=r#"steam_app_default|dragon age the veilguard.exe"#
        open-on-workspace "G"
        variable-refresh-rate true
        open-fullscreen true
    }
    
    window-rule {
      match app-id="steam" title=r#"^notificationtoasts_\d+_desktop$"#
      default-floating-position x=10 y=10 relative-to="bottom-right"
    }
 
    window-rule {
        match title="Avowed "
        open-on-workspace "G"
        variable-refresh-rate false
        open-fullscreen true
    }
    
    window-rule {
        match app-id="cyberpunk2077.exe"
        open-on-workspace "G"
        variable-refresh-rate false
        open-fullscreen true
    }
    
    layer-rule {
        match namespace="^noctalia-wallpaper*"
        place-within-backdrop true
    }
